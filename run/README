* Installing libraries and preprocessing datasets:
	1) make CORES={Available Cores}
		
		Example: make CORES=8
		
* Running an experiment:
	1) Change the working-dir parameter in the beginning of the experiment 
	   config file (e.g. exp.univ.hiero/config) with the absolute path of 
	   the experiment directory.
		
		Example: working-dir = /scratch/1/ahan/test/tr-en-edu/run/exp.univ.hiero
	
	2)./run-experiment.sh {experiment-directory}
		
		Example: ./run-experiment.sh exp.univ.hiero